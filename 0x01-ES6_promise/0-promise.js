export default function getResponseFromAPI() {
    return new Promise((resolve, reject) => {
        // Your API call logic goes here
        // If the API call is successful, call resolve(data)
        // If the API call fails, call reject(error)
    });
}
